# Core API
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
pydantic==2.5.2

# --- VISION PROCESSING ---
Pillow==11.3.0

# --- MLOps Optimization: CPU-Only PyTorch ---
# Forces the Docker container to pull the ~150MB CPU wheel 
# instead of the ~2.5GB CUDA binary, enabling edge deployment.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.1+cpu
torchvision==0.16.1+cpu
numpy==1.26.4